# =================================================================
# == Environment Variables for Aces Unknown Frontend
# =================================================================
#
# This file serves as an example.
# Copy this file to .env.local and fill in your actual values.
#
# DO NOT COMMIT .env.local TO VERSION CONTROL.

# --- Solana Configuration ---
# The URL of the Solana RPC endpoint to connect to.
# For local development, this is typically http://127.0.0.1:8899
# For devnet, use a reliable provider like Helius or QuickNode.
NEXT_PUBLIC_SOLANA_RPC_URL="http://127.0.0.1:8899"

# --- Arcium Configuration ---
# The Arcium cluster offset for the target environment.
# This value is provided by the Arcium deployment process.
# Refer to Arcium documentation for devnet/testnet/mainnet offsets.
NEXT_PUBLIC_ARCIUM_CLUSTER_OFFSET="1116522165" # Example for devnet

# --- Supabase Configuration ---
# These values can be found in your Supabase project's API settings.
NEXT_PUBLIC_SUPABASE_URL="YOUR_SUPABASE_URL"
NEXT_PUBLIC_SUPABASE_ANON_KEY="YOUR_SUPABASE_ANON_KEY"

# This is a secret key for performing admin-level operations in server actions.
# It should ONLY be used on the server and must not have the NEXT_PUBLIC_ prefix.
# Found in your Supabase project's API settings (service_role key).
SUPABASE_SERVICE_ROLE_KEY="YOUR_SUPABASE_SERVICE_ROLE_KEY"

# --- Supabase Storage Configuration ---
# The name of the public bucket used for storing user profile avatars.
NEXT_PUBLIC_AVATARS_BUCKET_NAME="profile-avatars"

# --- Database Configuration (for Drizzle ORM) ---
# The connection string for your Supabase Postgres database.
# Found in your Supabase project's Database settings.
# The [YOUR-PASSWORD] placeholder must be replaced with your actual database password.
DATABASE_URL="postgresql://postgres:[YOUR-PASSWORD] @db.your-project-ref.supabase.co:5432/postgres"